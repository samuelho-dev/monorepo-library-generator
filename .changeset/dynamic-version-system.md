---
"@samuelho-dev/monorepo-library-generator": patch
---

Fix version reporting in CLI and MCP binaries - now dynamically reads from package.json instead of hardcoded values. Adds automatic version.ts generation during build process.
