import { Schema } from "effect";

/**
 * Payment Schemas
 *
 * Shared schema definitions using Effect Schema.
 *
 */


// TODO: Add domain schemas

export const PaymentConfigSchema = Schema.Struct({
  // Add configuration schema fields
});

export const PaymentResultSchema = Schema.Struct({
  // Add result schema fields
});
