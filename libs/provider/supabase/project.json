{
  "name": "provider-supabase",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/provider/supabase/src",
  "tags": [
    "type:provider",
    "scope:supabase",
    "provider",
    "effect",
    "builtin"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/libs/provider/supabase",
        "tsConfig": "libs/provider/supabase/tsconfig.lib.json",
        "main": "libs/provider/supabase/src/index.ts",
        "batch": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "libs/provider/supabase/src/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": [
        "{options.reportsDirectory}"
      ],
      "options": {
        "reportsDirectory": "coverage/libs/provider/supabase",
        "config": "libs/provider/supabase/vitest.config.ts"
      }
    }
  }
}
