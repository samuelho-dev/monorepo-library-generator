{
  "name": "infra-observability",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/infra/observability/src",
  "tags": [
    "type:infra",
    "scope:observability",
    "infra",
    "orchestration",
    "builtin"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/libs/infra/observability",
        "tsConfig": "libs/infra/observability/tsconfig.lib.json",
        "main": "libs/infra/observability/src/index.ts",
        "batch": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "libs/infra/observability/src/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": [
        "{options.reportsDirectory}"
      ],
      "options": {
        "reportsDirectory": "coverage/libs/infra/observability",
        "config": "libs/infra/observability/vitest.config.ts"
      }
    }
  }
}
