{
  "name": "infra-pubsub",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/infra/pubsub/src",
  "tags": [
    "type:infra",
    "scope:pubsub",
    "infra",
    "orchestration",
    "builtin"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/libs/infra/pubsub",
        "tsConfig": "libs/infra/pubsub/tsconfig.lib.json",
        "main": "libs/infra/pubsub/src/index.ts",
        "batch": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "libs/infra/pubsub/src/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": [
        "{options.reportsDirectory}"
      ],
      "options": {
        "reportsDirectory": "coverage/libs/infra/pubsub",
        "config": "libs/infra/pubsub/vitest.config.ts"
      }
    }
  }
}
