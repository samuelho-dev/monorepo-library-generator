import { createToolDefinition } from "./blueprint.schema"

export const FeatureToolDefinition = createToolDefinition(
  "feature",
  "generate_feature",
  "Generate Pattern A feature services with router-owned composition"
)
