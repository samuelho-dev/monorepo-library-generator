{
  "$schema": "https://json-schema.org/schema",
  "$id": "Contract",
  "title": "Contract Library Generator",
  "type": "object",
  "cli": "nx",
  "description": "Generate a contract library following Effect-based architecture patterns",
  "properties": {
    "name": {
      "type": "string",
      "description": "Domain name (e.g., 'product', 'user', 'product-review')",
      "pattern": "^[a-z][a-z0-9-]*[a-z0-9]$",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the domain name for this contract?"
    },
    "directory": {
      "type": "string",
      "description": "Directory where the contract library will be created",
      "default": "libs/contract"
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of the domain",
      "x-prompt": "Provide a description for this domain"
    },
    "dependencies": {
      "type": "array",
      "description": "Contract dependencies (e.g., ['common', 'messaging'])",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "entities": {
      "type": "string",
      "description": "Comma-separated list of entity names for bundle optimization (e.g., 'Product,ProductCategory,ProductReview')"
    },
    "includeCQRS": {
      "type": "boolean",
      "description": "Include CQRS pattern files (commands, queries, projections)",
      "default": false,
      "x-prompt": "Include CQRS pattern files (commands, queries, projections)?"
    }
  },
  "required": ["name"]
}
